请完整阅读并遵从 D:\Projects\pto-design-system-wzh

包括
D:\Projects\pto-design-system-wzh\SKILL.md
D:\Projects\pto-design-system-wzh\references\DESIGN.md

页面必须使用'patterns/ide-frame'作为整体框架

必须优先复用现有pattern：
D:\Projects\pto-design-system-wzh\patterns\pattern.json

最后，按D:\Projects\pto-design-system-wzh\SKILL.md中的“工作流 B”优化 @xxx

如果任务过重，可以先列出步骤，我们一步一步完成



3d整网图

D:\Projects\pto-design-system-wzh\patterns\model-architecture-3d-deck\pattern.html


痛点：
做模型性能分析时，有些地方需要优化算子或做融合算子，但能优化多少很难评估，只能靠经验预估。英伟达Nsight可以看到算子实际搬运了多少数据，我们缺少这类数据，端到端指标很难往单算子拆

痛点：
即使找到已有算子，我也不知道它现在的性能水平，模型侧只有整体端到端目标，给不出这个算子应该达到多少，没有实测和基线，单算子性能指标很难制定

痛点：
算子报错信息经常不明显，有时plog里有有效信息，但Python测不会直接显示，用户还要自己去查plog。问了可能来自整网代码，也可能是某个shape不支持。当前报错往往不能说明具体原因。如果能直接指出那个参数的shape是什么，违法了什么约束，就能更快定位，参数使用的错位也能直接报出，避免完整跑完后才从精度异常中发现

痛点：
算子报错时，一般报的是Ascend C里面的变量名或者属性名，但我从torch_npu接口去用，名字和属性得想一想才对得上，不是那么直接，不知道设计算子时有没有规范，比如两个接口名字必须符合一定关系

痛点：
训练报错日志还是一样不清楚，每次都要去Device测plog grep error，感觉可以把有效信息抛到训练日志

痛点：
算子报HCCL通信错误或 corner ops 错误我们看不懂，只能指导框架哪块计算出错，一层层找框架负责人，算子接口人或看手册，定位好才提给算子开发，链路很长，短的两三天长的一周，没有提需求机制。找算子谁开发的也不好找，算子同事不一定及时支持。

痛点：
报错那句代码不一定是原因，可能是上游算子结果shape不对 dtype 不一样或内存越界。从框架侧根据报错定位范围，逻辑没问题就逐行打印 shape dtype ,看过哪个算子后不对。大规模任务还要排队，debug很费时间

月之暗面优秀实践：
NCCL的保持都很清楚， Unreachable 还是 Wrong Rank 都会给清楚的信号， Flight Recorder 直接拆解通信过程；框架层面DDP信号不同步，显存越界，设备错配，直接看到非常详细的 Python traceback 自动推飞书；算子层Compute Sanitizer精准定位非法内存访问、溢出、index越界也写得很清楚，基本2小时内可以完成捕捉、锁定、修复和闭环


痛点：
现在花的比较多的时间是精度问题，基本没有专用工具，都是逐层dump数据，然后和golden逐层比对，可能用torch脚本去比。算子交付后端到端精度、性能测试，出现精度问题要去定位，定位精度是个特别困难的事情

痛点
比较容易出现切分不对，或者有些融合算子不满足约束条件，可需要几个迭代，一开始打得粗一点再打到细节点，打印完还要识别到哪一行代码或哪个算子有问题；打印出来之后还要自己写工具做 tensor 对比，算矩阵相似度，如果能直接可视化整网模型、对应到代码，点选节点自动加dump，再可视化对比gloden差异，会方便很多

痛点
工具采集NPU和 GPU数据生成Excel比对会大面积标红，但标出来后还要在代码里打点打印张量自己比对，标红的地方不一定是精度问题产生处，异常可能出现在标红之前，要一步一步往前定位。客户的问题光迁移和精度定位就花了2个月，后一个月用各种工具对齐仍不完美。

月之暗面优秀实践
Tensorboard 检测到国产卡loss曲线和NV偏离度超越阈值就局部熔断，然后在测试床上两端加载完全相同的权重和输入，强制指向前向和反向传播；用自研Tensor Hook 注入脚本，不破坏原有计算图，自动拦截每一层 Transformer Layer的输出，在逐层自动化比对两端Tensor的余弦相似度和绝对误差，相似度低于0.999就锁定精度分叉在哪一层
