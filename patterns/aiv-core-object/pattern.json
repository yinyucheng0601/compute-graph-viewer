{
  "id": "aiv-core-object",
  "name": "AIV Core Object",
  "type": "hybrid",
  "description": "Design-driven AIV internal object pattern with cache cards, a Scalar bar, a Unified Buffer card, SIMT and SIMD execution cards, and a Vector compute block. Routes follow official Ascend AIV topology notes for shared ICache, shared Vector execution, Unified Buffer staging, and SIMT DCache flow.",
  "source": {
    "visualContract": [
      "user-supplied AIV draft in conversation",
      "official Ascend documentation for AIV topology and SIMT/SIMD shared resources",
      "patterns/aiv-core-object/pattern.html",
      "patterns/aiv-core-object/pattern.css",
      "patterns/aiv-core-object/pattern.js"
    ],
    "patternHtml": "patterns/aiv-core-object/pattern.html",
    "patternCss": "patterns/aiv-core-object/pattern.css",
    "renderScript": "patterns/aiv-core-object/pattern.js",
    "defaultPreset": "aivOfficialV1"
  },
  "useWhen": [
    "building an AIV internal memory and vector object in PTO",
    "creating architecture pages that need a dark Ascend-style vector-core composition",
    "reusing AIV shell layout and routes without hand-copying static HTML"
  ],
  "requiredApis": [
    "window.PtoAivCorePattern.render(container, presetOrKey)"
  ],
  "allowedOverrides": [
    "title, labels, capacities, and card membership",
    "per-card frame width and height",
    "grid rows, cols, cell size, gap, and highlighted band columns",
    "route list, route color family, and route anchor bias"
  ],
  "forbiddenOverrides": [
    "rewriting the shell layout in page-local CSS",
    "replacing official topology-derived routes with ad hoc demo lines",
    "hard-coding a one-off copy of generated DOM instead of using the shared renderer"
  ],
  "agentReuseRule": "Read this JSON first. For a new page, load pattern.js and call window.PtoAivCorePattern.render with a preset or config object. Extend the preset data for future chip variants instead of cloning the generated DOM."
}
