把所有文字转canvas，优化文字排版，不改内容：# AI Agent 对计算领域开发者 UX 影响的研究框架

## 核心洞见（5 项）

**Insight 1：调试的高认知成本是最大体验瓶颈，AI Agent 可显著降低该成本**

计算领域开发者的调试工作本质上是在非透明系统中进行归因分析——从 Python 源码到 Pass IR 再到 Swimlane，中间跨越 4-5 层非线性编译器重构。当前这些跃迁完全依赖经验积累完成，AI Agent 首次将隐式跃迁转化为显式可解释过程。

**Insight 2：从"工具集"到"智能向导"的形态转变是计算领域 UX 演进的核心路径**

PTO 的困境具有典型性：功能完备但以功能模块为单位组织，而非以用户任务流为单位。AI Agent 首次实现真正的"向导"模式——工具能够感知用户在分析旅程中的当前阶段，主动推荐后续步骤。

**Insight 3：领域知识获取范式发生根本性转变——从"被动阅读"转向"主动对话式积累"**

PTO 业务理解文件夹包含 15 篇以上研究笔记，覆盖 CANN 编译链路、tiling 策略、子图切分等主题。传统方式需数月知识浸泡方可建立，而今笔记本身即由 AI Agent 协作构建。知识壁垒正在消解，"谁能使用该工具"的边界被重定义。

**Insight 4：跨层级信息导航问题的复杂度超越可视化本身，亟需 Agent 介入**

用户核心需求是"追踪数据完整旅程"——而非在 7 个独立工具间切换。AI Agent 的关键价值在于维护跨层上下文，替代用户管理 rootHash / callOpMagic / semantic_label 三套 ID 系统的对应关系。

**Insight 5：新型用户角色正在形成——"设计思维驱动的工具构建者"**

PTO 本身即为例证：体验设计师不具备 CANN 背景，却构建出可供算子开发者使用的调试工具。AI Agent 解锁该可能性后，计算领域工具的 UX 质量将系统性地超越纯工程视角构建的工具。

---

## 体验转变维度分析（5 组）

| 维度 | 演变前 | 演变后 | PTO 应用场景 |
|------|--------|--------|--------------|
| 1 | 分散工具集 | 统一旅程工作台 | Swimlane 气泡 → Execute Graph 自动定位 → Block Graph 下钻 → 源码行回溯，全程不离工作台 |
| 2 | 现象观测 | 原因诊断 | 气泡密度异常 → 工具生成"诊断卡片"：vec_tile 配置不一致导致 op 未合并进同构子图，建议统一为 64 |
| 3 | 问题驱动检索 | 上下文持续维护 | 分析 Journal：关键操作记录为可复现快照，再次打开时恢复上次上下文状态 |
| 4 | 被动等待操作 | 主动识别入口 | 文件加载后自动生成健康报告："AIV 利用率 28%，主要瓶颈在 AIC_7 与 AIV_3，建议优先排查" |
| 5 | 领域专家专用 | 可解释协作工具 | Pass_33 切换时，工具说明："RemoveAlloc Pass，特定 tensor 在此消失属预期行为" |

---

## PTO 当前价值定位

**核心贡献：将"不可见计算过程"转化为"可见结构表示"**

- 编译器 JSON → 交互式 DAG 可视化
- 语义颜色分层（pipeline stage / op 类型 / 执行单元）
- Pass 时间线导航，跨阶段比较可操作化
- 脉道图使硬件执行时序可读

**目标用户群体：** PyPTO 算子开发者，具备源码、Pass 产物、脉道图等材料，缺乏整合工作台。典型应用场景：精度调试与性能调优。

**当前体验局限：** 三个视图作为独立工具存在，而非统一旅程的多视角呈现。能够展示"图结构"，但无法解释"图为何呈现该结构"。

---

## PTO 演进方向（优先级排序）

### 方向 1：从分散视图 → 统一工作台

左侧旅程树（模型层→源码层→编译层→执行层），中间主画布随层级缩放，右侧证据面板显示对象跨层关联。层级切换无需页面跳转。

技术基础已具备：sessionStorage bridge、program.json 跨视图绑定、iframe 分屏原型。缺失统一 UI 容器与状态管理层。

### 方向 2：从"结构展示" → "因果解释"（最大体验优化机会）

三层递进实现：

- **诊断提示层（规则库）：** 单语句识别异常模式
- **因果解释层（tile 配置关联）：** 候选原因 + 后续动作建议
- **AI 辅助 tiling 层（长期差异化）：** 基于图结构 + shape + profile 历史给出 tile 调整建议，附带解释

参考实现：Penzai 非破坏性 tensor 拦截、TensorBoard Debugger V2 的 NaN/Inf 传播追踪。

### 方向 3：从单次分析 → 可复现分析 Journal

每个关键发现携带当时的视图状态。Journal 积累后，AI Agent 可进行类比推荐："当前气泡分布与 QuantIndexerProlog 案例相似，上次解决方案为 vec_tile 参数调整。"

### PTO 作为"优秀实践"原型的定位

当前市场计算领域工具（MindInsight、Nsight、VTune、Perfetto）均由工程师按工程需求构建——功能完备但缺失用户旅程设计。PTO 有机会成为首个以"用户旅程"为核心组织原则的计算图调试工具，验证"体验设计 + AI Agent + 领域文档"可产出超越传统工程路径的工具质量。

---

## 核心研究命题（精简版）

> 当 AI Agent 能够充当"编译器知识翻译者"时，计算领域开发者工具的设计语言应如何重构——工具是否仍需将内部状态完全暴露给用户，还是应开始为用户进行抽象与过滤，仅呈现与当前问题相关的证据？

PTO 当前为"全量信息呈现"工具，但 PRD 与研究笔记已在探索"以问题为中心的证据工作台"。两种设计哲学间的张力构成了该领域最具深度的 UX 研究命题。

---

## 待验证关键假设

1. **向导需求验证：** 资深算子开发者是否会因向导化而感知灵活性受限？
2. **跨层跳转痛点验证：** 跨层跳转断点是否为最强体验痛点，还是用户已习惯多标签页工作流？
3. **诊断卡片可信度验证：** "原因卡片"解释的可信度是否达到可采纳阈值？需在真实 DeepSeek 调试案例中验证。

## 值得深入的研究方向

- **跨阶段对象身份映射：** 三套 ID 系统的稳定对应关系是跨层联动的基础设施
- **Agent 介入策略：** 介入时机与粒度优化（参考 GitHub Copilot 演进路径）
- **多卡并行拓扑可视化：** tp=8 多卡场景通信时序缺乏可读工具，属于严重低估的空白领域
- **vibe coding 工具构建模式：** 设计师 + Agent 协作长期维护计算工具技术正确性的可持续性研究

---

*v1.0 | 2026-03-30 | 基于 PTO 工程文件及业务理解文档*

---

## 补充维度：AI 变革涌现的新工具、新趋势、新交互（2024-2025）

### 一、工具层创新扫描——各工具的 UX 核心突破

**Google Stitch**

将"对话"转化为设计迭代主界面。用户不再在工具内"操作"，而是与工具"协商"——"生成三套配色方案"、"适配桌面端"。关键产品决策：生成产物直导 Figma，定位为"起点"而非"终点"。2026 年 3 月版本引入无限画布 + 上下文感知 Agent，完成从"生成工具"向"持续协作空间"的转型。

**v0 by Vercel**

将 React 组件体系转化为自然语言接口。用户描述意图而非界面，v0 在 shadcn/ui + Tailwind 约束框架内实例化。AutoFix 机制在生成中途自动修复，减少人工介入断点。定位明确：专精"标准化重复 UI 模式"，不追求全能工具定位。

**Figma Make**

消除"设计→原型"间的工具切换摩擦。基于 Claude 3.7 Sonnet，直接理解设计系统 token——生成原型采用用户品牌色、组件与间距规范，而非泛化 Bootstrap 样式。核心体验转变：验证想法从"半天原型搭建"降至"三句话加一次确认"。

**Lovable**

将产品经理角色嵌入 AI——主动提出澄清性问题，定位为产品伙伴而非代码工具，代码为副产品。2025 年底达到 200M ARR，表明"对话式产品构建"市场空间远超预期。

**Windsurf**

采取反直觉设计：默认隐藏 diff，优先展示运行结果。生成结果直写磁盘，开发服务器实时热重载，"想象→验证"缩短至秒级。这是"Human as Reviewer"范式的早期探索。

**Nsight Copilot（NVIDIA）**

计算领域 AI 化的直接案例。检测到 uncoalesced memory access 时，主动提供交互式优化引导——不仅指出问题，更解释原因并给出具体建议。CUDA 专用 LLM + ComputeEval 评测框架，表明该方向需领域专用模型，通用 LLM 能力不足。

**W&B Weave**

将 LLM 调用链的 tracing 与 eval 设为一等公民。从"比较两次训练曲线"转变为"理解 Agent 在多轮对话中的决策机制"。2025 年被 CoreWeave 收购，表明计算基础设施公司开始将 observability 视为核心竞争力。

---

### 二、跨工具收敛范式（5 项）

**范式 1：对话取代操作（Conversation over Operation）**

所有工具均在将"菜单/按钮/参数"替换为"意图表达"。新 UX 挑战：意图误解的回退机制、"不知道可请求什么"的冷启动问题。优秀工具（Lovable、Figma Make）通过主动提问解决。

**范式 2：反馈回路极度压缩（Instant Feedback Loop）**

反馈足够快时，迭代本身成为思考工具。这不只是速度提升，而是认知流程重组——秒级反馈彻底改变"先想清楚再动手"的工作模式，转向"边生成边收敛"。

**范式 3：人从"执行者"转变为"审核者"（Human as Reviewer）**

人不再编写代码/搭建界面，而是审查 diff、审批方向、划定决策边界。该角色转变带来新界面需求：变更可视化、意图解释、风险标注——现有工具在此方向尚处早期。

**范式 4：上下文工程成为核心能力（Context Engineering）**

大上下文窗口使 Agent 能持续维护项目状态，但"将所有信息塞入 context"属反模式。收敛最佳实践：稳定前缀（项目规范/约束）+ 动态后缀（当前任务）+ 外部记忆（RAG/文件系统）。CLAUDE.md 等规范文件的出现体现了该趋势在工具层的落地。

**范式 5：平台成为生态（Platform as Ecosystem）**

Lovable 预置 Supabase 后端、Figma Make 接入 Claude API、Claude Code 支持 MCP 扩展。MCP 作为标准化接口的出现，标志着 AI 工具生态从孤立运行进入互联互通阶段。